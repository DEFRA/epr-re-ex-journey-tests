.DS_Store
.npm/
.vscode
.idea
.server
node_modules
npm-debug.log
coverage
.cache
.png
allure-report/
allure-results/
test-artifacts/
test-results/
FAILED
# summarylogs-spreadsheet-data-generator.js's transient generated output -
# distinct from resources/templates/, which holds its committed source
# templates.
data/

# The simulated clock's state, written by `npm run clock` and read by every
# Node process in the stack.
test/support/simulator/clock/clock.txt*
